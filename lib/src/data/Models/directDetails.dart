// ignore_for_file: prefer_typing_uninitialized_variables, camel_case_types

class directDetails {
  var distance;
  var duration;
  var encodedpoints;

  directDetails({this.distance, this.duration, this.encodedpoints});
}
